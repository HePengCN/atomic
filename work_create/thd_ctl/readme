   1,所有控制函数，如果出现在不同线程中，请自己保证调用顺讯的有效和同步，比如线程a start确认成功的前提下，再让线程b 进行stop。 不然出现的结果不保证。
   2， 没有把 task_init 和 task_clear 放在锁内，是因为如果这些函数阻塞了，控制函数收不到反馈一直wait，但这时锁没有锁着，所以可以依靠其他线程来查看状态等操作。
   3， 4个控制函数，create， start， stop， destroy都建议用死等的方式，接口可以留出来，但使用强烈建议用死等，因为不死等线程状态不好确定。
